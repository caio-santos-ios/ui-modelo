import { atom } from "jotai";

export const accountPayableModalAtom = atom<boolean>(false);
export const accountPayableIdAtom = atom<string>("");
export const accountPayablePayModalAtom = atom<boolean>(false);
